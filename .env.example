# PostgreSQL connection string (Vercel Postgres / Prisma Postgres)
# e.g. postgres://user:password@host:5432/database?sslmode=require
DATABASE_URL="postgres://..."

# Secret used to sign JWT session tokens (keep identical across environments,
# otherwise existing authenticated sessions are invalidated)
JWT_SECRET="change-me-to-a-long-random-string"